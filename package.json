{
  "name": "sage-gui",
  "version": "2.1.0",
  "description": "",
  "scripts": {
    "start": "npm start -w sage",
    "build": "npm run build -w sage",
    "clean": "npm run clean --workspaces && rm -rf .parcel-cache/",
    "reg-api": "npm run start -w registration-api",
    "dev": "concurrently \"npm run reg-api\" \"npm start\"",
    "release": "standard-version"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sagecontinuum/sage-gui"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "process": "^0.11.10",
    "standard-version": "^9.3.0"
  },
  "standard-version": {
    "header": "# Changelog\n\nThe following are some notable changes to the UI\n\n"
  },
  "workspaces": [
    "apps/admin",
    "apps/sage",
    "apps/project",
    "apps/registration-api"
  ],
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
